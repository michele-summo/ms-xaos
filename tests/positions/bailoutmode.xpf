; A bailout shape other than the circle, which is the only setting written
; conditionally: a position that does not ask for one must not carry the
; directive at all, and one that does must carry it and come back the same.
(initstate)
(formula 'mandel)
(maxiter 200)
(bailoutmode 2)
(view -0.5 0 3 3)
